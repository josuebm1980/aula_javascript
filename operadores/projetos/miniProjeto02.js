//MiniProjeto
//programa que faz as mesma função dos radares quando veiculo ultrapadaa velocidade e e perde pontos e o motorista perde pontos em sua carteira;
//velocidade máxima de até : 70km
//a cada 5km acima do veiculo você ganha 1 ponto;
//math.floor();
//caso pontos maior >12 : 'carteira suspendida';

verificarvelocidade(130);

/*
function verificarvelocidade(velocidade){
    const velocidadeMaxima = 70;
    const kmPorPonto = 5;
    if (velocidade <= velocidadeMaxima)
          console.log('ok!');
        else{ 
        const pontos = Math.floor(((velocidade - velocidadeMaxima)/kmPorPonto));
          if(pontos >= 12)
            console.log('carteira suspensa');
            else
            console.log('pontos', pontos);

        }
}
*/
